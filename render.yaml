databases:
  - name: giras-db
    databaseName: giras
    user: giras
    plan: basic-256mb
    region: oregon
    postgresMajorVersion: "16"
    diskSizeGB: 1
    ipAllowList: []

services:
  - type: web
    name: giras-api
    runtime: python
    rootDir: backend
    plan: starter
    region: oregon
    healthCheckPath: /health
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
      - key: DATABASE_URL
        fromDatabase:
          name: giras-db
          property: connectionString
      - key: ENVIRONMENT
        value: production
      - key: CORS_ORIGINS
        value: https://giras-web.onrender.com,https://sistemainterno.markaplast.com.br
      - key: FISCAL_EMIT_ENABLED
        value: false
      - key: FOCUS_NFE_BASE_URL
        value: https://api.focusnfe.com.br/v2
      - key: AUTH_SECRET
        generateValue: true
      - key: AUTH_INITIAL_PASSWORD
        sync: false
      - key: AUTH_TOKEN_MINUTES
        value: 720

  - type: web
    name: giras-web
    runtime: static
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://giras-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
